import { makeStyles } from '@material-ui/core/styles';

export const PostCard = makeStyles((theme) => ({ 
    root: {
        margin: '10px',
    },
    title: {
        display: 'flex',
        justifyContent: 'center'
    },
    description: {

    },
}));